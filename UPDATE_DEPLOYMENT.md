# 🔄 تحديث النسخة المرفوعة على السيرفر

## الطريقة 1: التحديث عبر Git (الأسرع والأسهل)

### الخطوة 1: على جهازك المحلي (Cursor)

```powershell
# افتح Terminal في Cursor واكتب:
cd c:\Users\TWc\Desktop\4048\nulip-new

# إذا لم يكن Git مُعدّاً، قم بإعداده:
git init
git remote add origin https://github.com/YOUR_USERNAME/nulip-inventory.git

# أضف جميع التغييرات
git add .

# احفظ التغييرات
git commit -m "تحديث النظام - $(Get-Date -Format 'yyyy-MM-dd HH:mm')"

# ارفع للمستودع
git push origin main
```

### الخطوة 2: على السيرفر (SSH)

```bash
# اتصل بالسيرفر
ssh root@YOUR_VPS_IP

# اذهب لمجلد المشروع
cd /home/cloudpanel/htdocs/YOUR_DOMAIN

# اسحب التحديثات
git pull origin main

# أعد تثبيت الحزم (إذا تغيرت)
npm ci

# أعد بناء المشروع
npm run build

# أعد تشغيل التطبيق
pm2 restart nulip-inventory

# تحقق من الحالة
pm2 status
```

---

## الطريقة 2: التحديث عبر SFTP (بدون Git)

### الخطوة 1: بناء المشروع محلياً

```powershell
cd c:\Users\TWc\Desktop\4048\nulip-new

# بناء المشروع
npm run build
```

### الخطوة 2: الملفات المطلوب رفعها

| المجلد/الملف | الوصف | مطلوب |
|-------------|-------|-------|
| `dist/` | ملفات البناء | ✅ نعم |
| `package.json` | الحزم | ✅ نعم |
| `package-lock.json` | قفل الإصدارات | ✅ نعم |
| `drizzle.config.ts` | إعدادات قاعدة البيانات | ✅ نعم |
| `shared/` | الملفات المشتركة | ✅ نعم |
| `migrations/` | إذا تغير المخطط | ⚠️ حسب الحاجة |
| `node_modules/` | الحزم | ❌ لا ترفع |
| `.env` | متغيرات البيئة | ❌ لا تستبدل |

### الخطوة 3: رفع الملفات عبر FileZilla

1. افتح **FileZilla**
2. اتصل بالسيرفر:
   - Host: `YOUR_VPS_IP`
   - Port: `22`
   - Protocol: `SFTP`
3. انتقل إلى مجلد المشروع على السيرفر
4. ارفع الملفات (استبدل الموجود)

### الخطوة 4: على السيرفر بعد الرفع

```bash
ssh root@YOUR_VPS_IP
cd /home/cloudpanel/htdocs/YOUR_DOMAIN

# تثبيت الحزم الجديدة
npm ci --production

# تحديث قاعدة البيانات (إذا تغير المخطط)
npm run db:push

# إعادة تشغيل التطبيق
pm2 restart nulip-inventory
```

---

## الطريقة 3: سكربت التحديث التلقائي (على السيرفر)

### إنشاء سكربت التحديث

```bash
# على السيرفر، أنشئ سكربت التحديث
cat > /home/cloudpanel/htdocs/YOUR_DOMAIN/update.sh << 'EOF'
#!/bin/bash
set -e

echo "=========================================="
echo "🔄 بدء تحديث نظام إدارة المخزون"
echo "=========================================="
echo ""

cd /home/cloudpanel/htdocs/YOUR_DOMAIN

echo "📥 جلب التحديثات من Git..."
git fetch origin
git reset --hard origin/main

echo ""
echo "📦 تثبيت الحزم..."
npm ci

echo ""
echo "🔨 بناء المشروع..."
npm run build

echo ""
echo "🗄️ تحديث قاعدة البيانات..."
npm run db:push || echo "⚠️ لم يتم تحديث قاعدة البيانات (قد تكون محدثة)"

echo ""
echo "🔄 إعادة تشغيل التطبيق..."
pm2 restart nulip-inventory

echo ""
echo "=========================================="
echo "✅ تم التحديث بنجاح!"
echo "=========================================="
pm2 status
EOF

chmod +x /home/cloudpanel/htdocs/YOUR_DOMAIN/update.sh
```

### تشغيل التحديث

```bash
./update.sh
```

---

## 🚨 ملاحظات مهمة قبل التحديث

### 1. النسخ الاحتياطي (مهم جداً!)

```bash
# على السيرفر قبل التحديث
cd /home/cloudpanel/htdocs/YOUR_DOMAIN

# نسخ احتياطي لقاعدة البيانات
pg_dump -U YOUR_DB_USER YOUR_DB_NAME > backup_$(date +%Y%m%d_%H%M%S).sql

# نسخ احتياطي للملفات
cp -r dist dist_backup_$(date +%Y%m%d)
```

### 2. التحقق بعد التحديث

```bash
# تحقق من حالة التطبيق
pm2 status

# تحقق من السجلات
pm2 logs nulip-inventory --lines 20

# اختبر الموقع
curl -I https://YOUR_DOMAIN
```

### 3. في حال حدوث مشكلة

```bash
# استعادة النسخة السابقة
cd /home/cloudpanel/htdocs/YOUR_DOMAIN

# استعادة الملفات
rm -rf dist
mv dist_backup_YYYYMMDD dist

# إعادة تشغيل
pm2 restart nulip-inventory
```

---

## 📋 قائمة مراجعة التحديث

- [ ] نسخ احتياطي لقاعدة البيانات
- [ ] نسخ احتياطي للملفات المهمة
- [ ] بناء المشروع محلياً (`npm run build`)
- [ ] رفع الملفات للسيرفر
- [ ] تثبيت الحزم (`npm ci`)
- [ ] تحديث قاعدة البيانات (`npm run db:push`)
- [ ] إعادة تشغيل التطبيق (`pm2 restart`)
- [ ] اختبار الموقع
- [ ] التحقق من السجلات

---

*آخر تحديث: 2026-02-03*
