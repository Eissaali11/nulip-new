import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { ChartContainer, ChartTooltip } from "@/components/ui/chart";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuLabel,
  DropdownMenuSeparator,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { useAuth } from "@/lib/auth";
import dashboardBg from "@assets/image_1762515061799.png";
import type { DashboardStats, TechnicianWithFixedInventory, WarehouseWithStats } from "@shared/schema";
import { useQuery } from "@tanstack/react-query";
import { motion } from "framer-motion";
import {
  Activity,
  AlertTriangle,
  ArrowRight,
  Bell,
  CheckCircle,
  ClipboardCheck,
  Database,
  LayoutDashboard,
  LogOut,
  Package,
  RefreshCw,
  Smartphone,
  TrendingUp,
  TruckIcon,
  User,
  UserCircle,
  Users,
  Warehouse,
  Wrench,
  Zap
} from "lucide-react";
import { useEffect, useState } from "react";
import { Bar, BarChart, Cell, XAxis, YAxis } from "recharts";
import { Link, useLocation } from "wouter";

interface WarehouseTransfer {
  id: string;
  technicianId: string;
  status: 'pending' | 'accepted' | 'rejected';
}

// Circular Progress Component
const CircularProgress = ({
  percentage,
  size = 120,
  strokeWidth = 8,
  label,
  value,
  color = "#18B2B0",
  glowColor = "rgba(24, 178, 176, 0.4)"
}: {
  percentage: number;
  size?: number;
  strokeWidth?: number;
  label: string;
  value: string;
  color?: string;
  glowColor?: string;
}) => {
  const radius = (size - strokeWidth) / 2;
  const circumference = 2 * Math.PI * radius;
  const offset = circumference - (percentage / 100) * circumference;

  return (
    <div className="relative" style={{ width: size, height: size }}>
      <svg className="transform -rotate-90" width={size} height={size}>
        <defs>
          <filter id={`glow-${label}`}>
            <feGaussianBlur stdDeviation="4" result="coloredBlur" />
            <feMerge>
              <feMergeNode in="coloredBlur" />
              <feMergeNode in="SourceGraphic" />
            </feMerge>
          </filter>
        </defs>

        {/* Background circle */}
        <circle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          fill="none"
          stroke="rgba(255, 255, 255, 0.05)"
          strokeWidth={strokeWidth}
        />

        {/* Progress circle with enhanced animation */}
        <motion.circle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          fill="none"
          stroke={color}
          strokeWidth={strokeWidth}
          strokeDasharray={circumference}
          strokeDashoffset={offset}
          strokeLinecap="round"
          filter={`url(#glow-${label})`}
          initial={{ strokeDashoffset: circumference, opacity: 0 }}
          animate={{
            strokeDashoffset: offset,
            opacity: 1,
            rotate: [0, 360]
          }}
          transition={{
            strokeDashoffset: { duration: 2, ease: "easeOut" },
            opacity: { duration: 0.5 },
            rotate: { duration: 20, repeat: Infinity, ease: "linear" }
          }}
        />

        {/* Animated glow effect */}
        <motion.circle
          cx={size / 2}
          cy={size / 2}
          r={radius}
          fill="none"
          stroke={color}
          strokeWidth={strokeWidth * 0.5}
          strokeDasharray={circumference}
          strokeDashoffset={offset}
          strokeLinecap="round"
          opacity={0.3}
          initial={{ strokeDashoffset: circumference }}
          animate={{
            strokeDashoffset: offset,
            rotate: [0, -360]
          }}
          transition={{
            strokeDashoffset: { duration: 2, ease: "easeOut" },
            rotate: { duration: 15, repeat: Infinity, ease: "linear" }
          }}
        />
      </svg>

      <div className="absolute inset-0 flex flex-col items-center justify-center">
        <motion.div
          initial={{ scale: 0 }}
          animate={{ scale: 1 }}
          transition={{ delay: 0.3, type: "spring" }}
          className="text-center w-full px-3 flex flex-col items-center justify-center gap-1"
        >
          <div
            className="text-2xl lg:text-3xl font-bold leading-none"
            style={{ color }}
          >
            {value}
          </div>
          <div
            className="text-xs lg:text-sm font-semibold leading-tight"
            style={{ color }}
          >
            {label}
          </div>
        </motion.div>
      </div>
    </div>
  );
};

// Animated Grid Background
const GridBackground = () => (
  <div className="absolute inset-0 opacity-20">
    <div className="absolute inset-0"
      style={{
        backgroundImage: `
             linear-gradient(rgba(24, 178, 176, 0.1) 1px, transparent 1px),
             linear-gradient(90deg, rgba(24, 178, 176, 0.1) 1px, transparent 1px)
           `,
        backgroundSize: '50px 50px'
      }}
    />
  </div>
);

export default function Dashboard() {
  const { user, logout } = useAuth();
  const [, setLocation] = useLocation();
  const [currentTime, setCurrentTime] = useState(new Date());

  const { data: pendingTransfers = [] } = useQuery<WarehouseTransfer[]>({
    queryKey: ["/api/warehouse-transfers", user?.id],
    enabled: !!user?.id && user?.role !== 'admin',
    select: (data) => data.filter(t => t.status === 'pending' && t.technicianId === user?.id),
  });

  const { data: dashboardStats, isLoading: statsLoading } = useQuery<DashboardStats>({
    queryKey: ["/api/dashboard"],
  });

  const { data: techniciansData } = useQuery<{ technicians: TechnicianWithFixedInventory[] }>({
    queryKey: ["/api/admin/all-technicians-inventory"],
    enabled: user?.role === 'admin',
  });

  const { data: warehousesData } = useQuery<WarehouseWithStats[]>({
    queryKey: ["/api/warehouses"],
    enabled: user?.role === 'admin',
  });

  useEffect(() => {
    const timer = setInterval(() => setCurrentTime(new Date()), 1000);
    return () => clearInterval(timer);
  }, []);

  const handleLogout = async () => {
    await logout();
    setLocation("/");
  };

  // Calculate technician efficiency
  const getTechnicianEfficiency = (tech: TechnicianWithFixedInventory) => {
    if (!tech.fixedInventory) return 0;
    const totalItems =
      (tech.fixedInventory.n950Boxes || 0) + (tech.fixedInventory.n950Units || 0) +
      (tech.fixedInventory.i9000sBoxes || 0) + (tech.fixedInventory.i9000sUnits || 0) +
      (tech.fixedInventory.i9100Boxes || 0) + (tech.fixedInventory.i9100Units || 0) +
      (tech.fixedInventory.rollPaperBoxes || 0) + (tech.fixedInventory.rollPaperUnits || 0) +
      (tech.fixedInventory.stickersBoxes || 0) + (tech.fixedInventory.stickersUnits || 0) +
      (tech.fixedInventory.newBatteriesBoxes || 0) + (tech.fixedInventory.newBatteriesUnits || 0) +
      (tech.fixedInventory.mobilySimBoxes || 0) + (tech.fixedInventory.mobilySimUnits || 0) +
      (tech.fixedInventory.stcSimBoxes || 0) + (tech.fixedInventory.stcSimUnits || 0) +
      (tech.fixedInventory.zainSimBoxes || 0) + (tech.fixedInventory.zainSimUnits || 0);

    const threshold = tech.fixedInventory.criticalStockThreshold || 100;
    return Math.min(100, Math.round((totalItems / threshold) * 100));
  };

  // Calculate global efficiency
  const globalEfficiency = techniciansData?.technicians
    ? Math.round(
      techniciansData.technicians.reduce((sum, tech) => sum + getTechnicianEfficiency(tech), 0) /
      techniciansData.technicians.length
    )
    : 0;

  // Get warehouse total units
  const getWarehouseTotalUnits = (warehouse: WarehouseWithStats) => {
    if (!warehouse.inventory) return 0;
    const inv = warehouse.inventory;
    return (
      (inv.n950Boxes || 0) + (inv.n950Units || 0) +
      (inv.i9000sBoxes || 0) + (inv.i9000sUnits || 0) +
      (inv.i9100Boxes || 0) + (inv.i9100Units || 0) +
      (inv.rollPaperBoxes || 0) + (inv.rollPaperUnits || 0) +
      (inv.stickersBoxes || 0) + (inv.stickersUnits || 0) +
      (inv.newBatteriesBoxes || 0) + (inv.newBatteriesUnits || 0) +
      (inv.mobilySimBoxes || 0) + (inv.mobilySimUnits || 0) +
      (inv.stcSimBoxes || 0) + (inv.stcSimUnits || 0) +
      (inv.zainSimBoxes || 0) + (inv.zainSimUnits || 0)
    );
  };

  return (
    <div
      className="min-h-screen text-white overflow-hidden relative"
      dir="rtl"
      style={{
        backgroundImage: `url(${dashboardBg})`,
        backgroundSize: 'cover',
        backgroundPosition: 'center',
        backgroundRepeat: 'no-repeat',
        backgroundAttachment: 'fixed'
      }}
    >
      {/* Dark Overlay for better readability */}
      <div className="absolute inset-0 bg-gradient-to-b from-[#050508]/90 via-[#050508]/85 to-[#050508]/90 backdrop-blur-[2px] z-0" />
      <GridBackground />

      {/* Futuristic Header */}
      <div className="relative z-10 border-b border-[#18B2B0]/20 bg-gradient-to-r from-[#0a0a0f]/90 via-[#0f0f15]/90 to-[#0a0a0f]/90 backdrop-blur-md">
        <div className="container mx-auto px-4 py-4">
          <div className="flex items-center justify-between">
            <div className="flex items-center gap-4">
              <motion.div
                className="relative p-3 bg-gradient-to-br from-[#18B2B0] to-[#0ea5a3] rounded-lg"
                animate={{
                  boxShadow: [
                    "0 0 20px rgba(24, 178, 176, 0.3)",
                    "0 0 40px rgba(24, 178, 176, 0.5)",
                    "0 0 20px rgba(24, 178, 176, 0.3)"
                  ]
                }}
                transition={{ duration: 2, repeat: Infinity }}
              >
                <LayoutDashboard className="h-6 w-6 text-white" />
              </motion.div>
              <div>
                <h1 className="text-2xl font-bold bg-gradient-to-r from-[#18B2B0] via-[#0ea5a3] to-[#18B2B0] bg-clip-text text-transparent">
                  QUANTUM STOCKFLOW
                </h1>
                <p className="text-xs text-gray-400 font-mono">
                  {currentTime.toLocaleTimeString('en-US', { hour12: false })} • SYSTEM ONLINE
                </p>
              </div>
            </div>

            <DropdownMenu>
              <DropdownMenuTrigger asChild>
                <motion.button
                  className="flex items-center gap-3 bg-white/5 hover:bg-white/10 backdrop-blur-sm rounded-lg px-4 py-2 transition-all border border-[#18B2B0]/20"
                  whileHover={{ scale: 1.05, borderColor: "rgba(24, 178, 176, 0.5)" }}
                  whileTap={{ scale: 0.95 }}
                >
                  <UserCircle className="h-5 w-5 text-[#18B2B0]" />
                  <div className="hidden sm:block text-right">
                    <p className="text-white font-semibold text-sm">{user?.fullName}</p>
                    <p className="text-gray-400 text-xs">
                      {user?.role === 'admin' ? 'مدير' : 'فني'}
                    </p>
                  </div>
                </motion.button>
              </DropdownMenuTrigger>
              <DropdownMenuContent className="w-56 bg-[#0f0f15] border-[#18B2B0]/20 backdrop-blur-xl" align="start">
                <DropdownMenuLabel className="text-white">الحساب</DropdownMenuLabel>
                <DropdownMenuSeparator className="bg-[#18B2B0]/20" />
                <DropdownMenuItem className="cursor-default focus:bg-white/5">
                  <div className="flex items-center gap-2 w-full">
                    <User className="h-4 w-4 text-[#18B2B0]" />
                    <div className="flex-1 text-right">
                      <p className="text-sm font-medium text-white">{user?.fullName}</p>
                      <p className="text-xs text-gray-400">@{user?.username}</p>
                    </div>
                  </div>
                </DropdownMenuItem>
                <DropdownMenuItem
                  className="cursor-pointer text-right focus:bg-red-500/20 text-red-400"
                  onClick={handleLogout}
                >
                  <div className="flex items-center gap-2 w-full justify-end">
                    <span className="font-medium">تسجيل الخروج</span>
                    <LogOut className="h-4 w-4" />
                  </div>
                </DropdownMenuItem>
              </DropdownMenuContent>
            </DropdownMenu>
          </div>
        </div>
      </div>

      <div className="container mx-auto px-4 sm:px-6 lg:px-8 py-6 sm:py-8 relative z-10">
        {/* Main Circular Dashboard */}
        <div className="grid grid-cols-1 lg:grid-cols-3 gap-6 lg:gap-8 mb-6 lg:mb-8">
          {/* Center: Main Circular Display */}
          <div className="lg:col-span-2">
            <motion.div
              initial={{ opacity: 0, y: 20 }}
              animate={{ opacity: 1, y: 0 }}
              className="relative bg-gradient-to-br from-white/10 to-white/[0.03] backdrop-blur-xl rounded-3xl border border-[#18B2B0]/30 p-6 lg:p-8 overflow-hidden shadow-2xl"
            >
              <div className="absolute inset-0 bg-gradient-to-br from-[#18B2B0]/10 to-transparent" />

              <div className="relative">
                <div className="flex items-center justify-between mb-6 lg:mb-8">
                  <div className="flex items-center gap-3">
                    <Activity className="h-5 w-5 lg:h-6 lg:w-6 text-[#18B2B0]" />
                    <h2 className="text-xl lg:text-2xl font-bold text-white">نظرة عامة على النظام</h2>
                  </div>
                  <Badge className="bg-green-500/20 text-green-400 border-green-500/30 text-xs lg:text-sm">
                    <div className="w-2 h-2 bg-green-400 rounded-full ml-2 animate-pulse" />
                    يعمل
                  </Badge>
                </div>

                {/* Circular Metrics Grid */}
                <div className="grid grid-cols-2 md:grid-cols-4 gap-6 lg:gap-8 justify-items-center mb-6 lg:mb-8">
                  <div className="flex flex-col items-center">
                    <CircularProgress
                      percentage={globalEfficiency}
                      label="الكفاءة"
                      value={`${globalEfficiency}%`}
                      color="#18B2B0"
                      glowColor="rgba(24, 178, 176, 0.4)"
                      size={120}
                    />
                  </div>

                  <div className="flex flex-col items-center">
                    <CircularProgress
                      percentage={dashboardStats ? Math.min(100, (dashboardStats.totalItems / 10000) * 100) : 0}
                      label="السعة"
                      value={dashboardStats ? `${dashboardStats.totalItems}` : "0"}
                      color="#0ea5a3"
                      glowColor="rgba(14, 165, 163, 0.4)"
                      size={120}
                    />
                  </div>

                  <div className="flex flex-col items-center">
                    <CircularProgress
                      percentage={dashboardStats && dashboardStats.totalItems > 0
                        ? 100 - ((dashboardStats.lowStockItems / dashboardStats.totalItems) * 100)
                        : 100}
                      label="مستوى المخزون"
                      value={dashboardStats ? `${dashboardStats.lowStockItems}` : "0"}
                      color="#f59e0b"
                      glowColor="rgba(245, 158, 11, 0.4)"
                      size={120}
                    />
                  </div>

                  <div className="flex flex-col items-center">
                    <CircularProgress
                      percentage={dashboardStats && dashboardStats.totalItems > 0
                        ? 100 - ((dashboardStats.outOfStockItems / dashboardStats.totalItems) * 100)
                        : 100}
                      label="التوفر"
                      value={dashboardStats ? `${100 - Math.round((dashboardStats.outOfStockItems / (dashboardStats.totalItems || 1)) * 100)}%` : "100%"}
                      color="#10b981"
                      glowColor="rgba(16, 185, 129, 0.4)"
                      size={120}
                    />
                  </div>
                </div>

                {/* Stats Bar */}
                {dashboardStats && (
                  <motion.div
                    initial={{ opacity: 0 }}
                    animate={{ opacity: 1 }}
                    transition={{ delay: 0.5 }}
                    className="grid grid-cols-2 md:grid-cols-4 gap-3 lg:gap-4"
                  >
                    <div className="bg-gradient-to-br from-[#18B2B0]/10 to-transparent border border-[#18B2B0]/20 rounded-xl p-3 lg:p-4">
                      <div className="flex items-center gap-2 mb-2">
                        <Database className="h-4 w-4 text-[#18B2B0]" />
                        <p className="text-xs text-gray-400">إجمالي الأصناف</p>
                      </div>
                      <p className="text-xl lg:text-2xl font-bold text-white">{dashboardStats.totalItems.toLocaleString()}</p>
                    </div>

                    <div className="bg-gradient-to-br from-orange-500/10 to-transparent border border-orange-500/20 rounded-xl p-3 lg:p-4">
                      <div className="flex items-center gap-2 mb-2">
                        <AlertTriangle className="h-4 w-4 text-orange-400" />
                        <p className="text-xs text-gray-400">مخزون منخفض</p>
                      </div>
                      <p className="text-xl lg:text-2xl font-bold text-orange-400">{dashboardStats.lowStockItems}</p>
                    </div>

                    <div className="bg-gradient-to-br from-red-500/10 to-transparent border border-red-500/20 rounded-xl p-3 lg:p-4">
                      <div className="flex items-center gap-2 mb-2">
                        <Zap className="h-4 w-4 text-red-400" />
                        <p className="text-xs text-gray-400">نفد المخزون</p>
                      </div>
                      <p className="text-xl lg:text-2xl font-bold text-red-400">{dashboardStats.outOfStockItems}</p>
                    </div>

                    <div className="bg-gradient-to-br from-green-500/10 to-transparent border border-green-500/20 rounded-xl p-3 lg:p-4">
                      <div className="flex items-center gap-2 mb-2">
                        <TrendingUp className="h-4 w-4 text-green-400" />
                        <p className="text-xs text-gray-400">اليوم</p>
                      </div>
                      <p className="text-xl lg:text-2xl font-bold text-green-400">{dashboardStats.todayTransactions}</p>
                    </div>
                  </motion.div>
                )}
              </div>
            </motion.div>
          </div>

          {/* Right: Quick Actions */}
          <div className="space-y-4 lg:space-y-6">
            <motion.div
              initial={{ opacity: 0, x: 20 }}
              animate={{ opacity: 1, x: 0 }}
              className="bg-gradient-to-br from-white/10 to-white/[0.03] backdrop-blur-xl rounded-3xl border border-[#18B2B0]/30 p-6 lg:p-8 shadow-xl"
            >
              <div className="flex items-center gap-3 mb-6 lg:mb-8">
                <motion.div
                  className="p-2.5 bg-gradient-to-br from-[#18B2B0]/20 to-[#0ea5a3]/20 rounded-xl border border-[#18B2B0]/30"
                  animate={{
                    boxShadow: [
                      "0 0 15px rgba(24, 178, 176, 0.2)",
                      "0 0 25px rgba(24, 178, 176, 0.4)",
                      "0 0 15px rgba(24, 178, 176, 0.2)"
                    ]
                  }}
                  transition={{ duration: 2, repeat: Infinity }}
                >
                  <Zap className="h-5 w-5 lg:h-6 lg:w-6 text-[#18B2B0]" />
                </motion.div>
                <h3 className="text-lg lg:text-xl font-bold text-white bg-gradient-to-r from-[#18B2B0] to-[#0ea5a3] bg-clip-text text-transparent">
                  الوصول السريع
                </h3>
              </div>

              <div className="space-y-4 lg:space-y-5">
                {user?.role !== 'admin' ? (
                  <>
                    <Link href="/my-fixed-inventory">
                      <motion.div
                        whileHover={{ scale: 1.02, y: -3 }}
                        whileTap={{ scale: 0.98 }}
                        className="group relative overflow-hidden rounded-2xl border border-[#18B2B0]/20 hover:border-[#18B2B0]/40 transition-all duration-300"
                      >
                        <div className="absolute inset-0 bg-gradient-to-r from-[#18B2B0]/10 via-[#0ea5a3]/10 to-[#18B2B0]/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <div className="absolute inset-0 bg-gradient-to-br from-[#18B2B0]/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <Button
                          className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-[#18B2B0]/20 group-hover:shadow-[#18B2B0]/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                        >
                          <div className="flex items-center justify-between w-full gap-4">
                            <div className="flex items-center gap-4">
                              <motion.div 
                                className="p-3 bg-gradient-to-br from-[#18B2B0]/20 to-[#0ea5a3]/20 rounded-xl border border-[#18B2B0]/30 group-hover:border-[#18B2B0]/50 transition-all"
                                whileHover={{ rotate: 12, scale: 1.1 }}
                              >
                                <Package className="h-5 w-5 lg:h-6 lg:w-6 text-[#18B2B0]" />
                              </motion.div>
                              <span className="font-bold text-base lg:text-lg">المخزون الثابت</span>
                            </div>
                            <motion.div
                              animate={{ x: [0, 4, 0] }}
                              transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                            >
                              <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                            </motion.div>
                          </div>
                        </Button>
                      </motion.div>
                    </Link>
                    <Link href="/my-moving-inventory">
                      <motion.div
                        whileHover={{ scale: 1.02, y: -3 }}
                        whileTap={{ scale: 0.98 }}
                        className="group relative overflow-hidden rounded-2xl border border-orange-500/20 hover:border-orange-500/40 transition-all duration-300"
                      >
                        <div className="absolute inset-0 bg-gradient-to-r from-orange-500/10 via-red-500/10 to-orange-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <div className="absolute inset-0 bg-gradient-to-br from-orange-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <Button
                          className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-orange-500/20 group-hover:shadow-orange-500/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                        >
                          <div className="flex items-center justify-between w-full gap-4">
                            <div className="flex items-center gap-4">
                              <motion.div 
                                className="p-3 bg-gradient-to-br from-orange-500/20 to-red-500/20 rounded-xl border border-orange-500/30 group-hover:border-orange-500/50 transition-all"
                                whileHover={{ rotate: 12, scale: 1.1 }}
                              >
                                <TruckIcon className="h-5 w-5 lg:h-6 lg:w-6 text-orange-400" />
                              </motion.div>
                              <span className="font-bold text-base lg:text-lg">المخزون المتحرك</span>
                            </div>
                            <motion.div
                              animate={{ x: [0, 4, 0] }}
                              transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                            >
                              <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                            </motion.div>
                          </div>
                        </Button>
                      </motion.div>
                    </Link>
                    {pendingTransfers.length > 0 && (
                      <Link href="/notifications">
                        <motion.div
                          whileHover={{ scale: 1.02, y: -3 }}
                          whileTap={{ scale: 0.98 }}
                          className="group relative overflow-hidden rounded-2xl border border-purple-500/20 hover:border-purple-500/40 transition-all duration-300"
                        >
                          <div className="absolute inset-0 bg-gradient-to-r from-purple-500/10 via-pink-500/10 to-purple-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                          <div className="absolute inset-0 bg-gradient-to-br from-purple-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                          <Button
                            className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-purple-500/20 group-hover:shadow-purple-500/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                          >
                            <div className="flex items-center justify-between w-full gap-4">
                              <div className="flex items-center gap-4">
                                <motion.div 
                                  className="p-3 bg-gradient-to-br from-purple-500/20 to-pink-500/20 rounded-xl border border-purple-500/30 group-hover:border-purple-500/50 transition-all relative"
                                  whileHover={{ rotate: 12, scale: 1.1 }}
                                >
                                  <Bell className="h-5 w-5 lg:h-6 lg:w-6 text-purple-400" />
                                  <motion.span 
                                    className="absolute -top-1 -right-1 bg-red-500 text-white text-[10px] rounded-full w-6 h-6 flex items-center justify-center font-bold shadow-lg"
                                    animate={{ scale: [1, 1.2, 1] }}
                                    transition={{ duration: 1.5, repeat: Infinity }}
                                  >
                                    {pendingTransfers.length}
                                  </motion.span>
                                </motion.div>
                                <span className="font-bold text-base lg:text-lg">الإشعارات</span>
                              </div>
                              <motion.div
                                animate={{ x: [0, 4, 0] }}
                                transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                              >
                                <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                              </motion.div>
                            </div>
                          </Button>
                        </motion.div>
                      </Link>
                    )}
                  </>
                ) : (
                  <>
                    <Link href="/admin-inventory-overview">
                      <motion.div
                        whileHover={{ scale: 1.02, y: -3 }}
                        whileTap={{ scale: 0.98 }}
                        className="group relative overflow-hidden rounded-2xl border border-[#18B2B0]/20 hover:border-[#18B2B0]/40 transition-all duration-300"
                      >
                        <div className="absolute inset-0 bg-gradient-to-r from-[#18B2B0]/10 via-[#0ea5a3]/10 to-[#18B2B0]/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <div className="absolute inset-0 bg-gradient-to-br from-[#18B2B0]/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <Button
                          className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-[#18B2B0]/20 group-hover:shadow-[#18B2B0]/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                        >
                          <div className="flex items-center justify-between w-full gap-4">
                            <div className="flex items-center gap-4">
                              <motion.div 
                                className="p-3 bg-gradient-to-br from-[#18B2B0]/20 to-[#0ea5a3]/20 rounded-xl border border-[#18B2B0]/30 group-hover:border-[#18B2B0]/50 transition-all"
                                whileHover={{ rotate: 12, scale: 1.1 }}
                              >
                                <Users className="h-5 w-5 lg:h-6 lg:w-6 text-[#18B2B0]" />
                              </motion.div>
                              <span className="font-bold text-base lg:text-lg">لوحة الفنيين</span>
                            </div>
                            <motion.div
                              animate={{ x: [0, 4, 0] }}
                              transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                            >
                              <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                            </motion.div>
                          </div>
                        </Button>
                      </motion.div>
                    </Link>
                    <Link href="/warehouses">
                      <motion.div
                        whileHover={{ scale: 1.02, y: -3 }}
                        whileTap={{ scale: 0.98 }}
                        className="group relative overflow-hidden rounded-2xl border border-orange-500/20 hover:border-orange-500/40 transition-all duration-300"
                      >
                        <div className="absolute inset-0 bg-gradient-to-r from-orange-500/10 via-red-500/10 to-orange-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <div className="absolute inset-0 bg-gradient-to-br from-orange-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <Button
                          className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-orange-500/20 group-hover:shadow-orange-500/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                        >
                          <div className="flex items-center justify-between w-full gap-4">
                            <div className="flex items-center gap-4">
                              <motion.div 
                                className="p-3 bg-gradient-to-br from-orange-500/20 to-red-500/20 rounded-xl border border-orange-500/30 group-hover:border-orange-500/50 transition-all"
                                whileHover={{ rotate: 12, scale: 1.1 }}
                              >
                                <Warehouse className="h-5 w-5 lg:h-6 lg:w-6 text-orange-400" />
                              </motion.div>
                              <span className="font-bold text-base lg:text-lg">المستودعات</span>
                            </div>
                            <motion.div
                              animate={{ x: [0, 4, 0] }}
                              transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                            >
                              <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                            </motion.div>
                          </div>
                        </Button>
                      </motion.div>
                    </Link>
                    <Link href="/operations">
                      <motion.div
                        whileHover={{ scale: 1.02, y: -3 }}
                        whileTap={{ scale: 0.98 }}
                        className="group relative overflow-hidden rounded-2xl border border-purple-500/20 hover:border-purple-500/40 transition-all duration-300"
                      >
                        <div className="absolute inset-0 bg-gradient-to-r from-purple-500/10 via-pink-500/10 to-purple-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <div className="absolute inset-0 bg-gradient-to-br from-purple-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                        <Button
                          className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-purple-500/20 group-hover:shadow-purple-500/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                        >
                          <div className="flex items-center justify-between w-full gap-4">
                            <div className="flex items-center gap-4">
                              <motion.div 
                                className="p-3 bg-gradient-to-br from-purple-500/20 to-pink-500/20 rounded-xl border border-purple-500/30 group-hover:border-purple-500/50 transition-all"
                                whileHover={{ rotate: 12, scale: 1.1 }}
                              >
                                <ClipboardCheck className="h-5 w-5 lg:h-6 lg:w-6 text-purple-400" />
                              </motion.div>
                              <span className="font-bold text-base lg:text-lg">العمليات</span>
                            </div>
                            <motion.div
                              animate={{ x: [0, 4, 0] }}
                              transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                            >
                              <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                            </motion.div>
                          </div>
                        </Button>
                      </motion.div>
                    </Link>
                  </>
                )}
                <motion.div
                  whileHover={{ scale: 1.02, y: -3 }}
                  whileTap={{ scale: 0.98 }}
                  className="group relative overflow-hidden rounded-2xl border border-green-500/20 hover:border-green-500/40 transition-all duration-300"
                >
                  <div className="absolute inset-0 bg-gradient-to-r from-green-500/10 via-emerald-500/10 to-green-500/10 opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                  <div className="absolute inset-0 bg-gradient-to-br from-green-500/20 to-transparent opacity-0 group-hover:opacity-100 transition-opacity duration-300" />
                  <Button
                    className="w-full relative bg-transparent hover:bg-transparent text-white border-0 shadow-lg shadow-green-500/20 group-hover:shadow-green-500/40 text-sm lg:text-base py-5 lg:py-6 px-4 lg:px-5 transition-all duration-300"
                    onClick={() => window.location.reload()}
                  >
                    <div className="flex items-center justify-between w-full gap-4">
                      <div className="flex items-center gap-4">
                        <motion.div 
                          className="p-3 bg-gradient-to-br from-green-500/20 to-emerald-500/20 rounded-xl border border-green-500/30 group-hover:border-green-500/50 transition-all"
                          whileHover={{ rotate: 180, scale: 1.1 }}
                          transition={{ duration: 0.5 }}
                        >
                          <RefreshCw className="h-5 w-5 lg:h-6 lg:w-6 text-green-400" />
                        </motion.div>
                        <span className="font-bold text-base lg:text-lg">إعادة الطلب التلقائي</span>
                      </div>
                      <motion.div
                        animate={{ x: [0, 4, 0] }}
                        transition={{ duration: 2, repeat: Infinity, ease: "easeInOut" }}
                      >
                        <ArrowRight className="h-5 w-5 opacity-70 group-hover:opacity-100 group-hover:translate-x-1 transition-all" />
                      </motion.div>
                    </div>
                  </Button>
                </motion.div>
              </div>
            </motion.div>

            {/* System Status */}
            <motion.div
              initial={{ opacity: 0, x: 20 }}
              animate={{ opacity: 1, x: 0 }}
              transition={{ delay: 0.2 }}
              className="bg-gradient-to-br from-white/10 to-white/[0.03] backdrop-blur-xl rounded-3xl border border-[#18B2B0]/30 p-5 lg:p-6 shadow-xl"
            >
              <h3 className="text-base lg:text-lg font-bold text-white mb-4 lg:mb-5 flex items-center gap-2">
                <Activity className="h-5 w-5 text-[#18B2B0]" />
                حالة النظام
              </h3>

              <div className="space-y-2.5 lg:space-y-3">
                <div className="flex items-center justify-between">
                  <span className="text-sm text-gray-400">قاعدة البيانات</span>
                  <Badge className="bg-green-500/20 text-green-400 border-green-500/30 text-xs">
                    <div className="w-2 h-2 bg-green-400 rounded-full ml-2 animate-pulse" />
                    متصل
                  </Badge>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-sm text-gray-400">خدمات API</span>
                  <Badge className="bg-green-500/20 text-green-400 border-green-500/30 text-xs">
                    <div className="w-2 h-2 bg-green-400 rounded-full ml-2 animate-pulse" />
                    نشط
                  </Badge>
                </div>
                <div className="flex items-center justify-between">
                  <span className="text-sm text-gray-400">حالة المزامنة</span>
                  <Badge className="bg-[#18B2B0]/20 text-[#18B2B0] border-[#18B2B0]/30 text-xs">
                    <div className="w-2 h-2 bg-[#18B2B0] rounded-full ml-2 animate-pulse" />
                    متزامن
                  </Badge>
                </div>
              </div>
            </motion.div>
          </div>
        </div>

        {/* Bottom: Technicians & Warehouses */}
        <div className="grid grid-cols-1 lg:grid-cols-2 gap-6 lg:gap-8">
          {/* Technicians */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.3 }}
            className="bg-gradient-to-br from-white/10 to-white/[0.03] backdrop-blur-xl rounded-3xl border border-orange-500/30 p-6 shadow-xl"
          >
            <div className="flex items-center gap-3 mb-6">
              <div className="p-2 bg-gradient-to-br from-orange-500 to-red-500 rounded-lg">
                <Wrench className="h-5 w-5 text-white" />
              </div>
              <h2 className="text-xl font-bold text-white">مجموعات الفنيين</h2>
            </div>

            <div className="space-y-3 mb-4">
              {user?.role === 'admin' && techniciansData?.technicians ? (
                <>
                  {techniciansData.technicians.slice(0, 3).map((tech, index) => {
                    const efficiency = getTechnicianEfficiency(tech);
                    const alertLevel = tech.alertLevel || 'good';

                    return (
                      <motion.div
                        key={tech.technicianId}
                        initial={{ opacity: 0, x: -20 }}
                        animate={{ opacity: 1, x: 0 }}
                        transition={{ delay: index * 0.1 }}
                        className="bg-white/5 backdrop-blur-sm rounded-xl border border-white/10 p-4 hover:border-[#18B2B0]/30 transition-all"
                      >
                        <div className="flex items-center justify-between">
                          <div className="flex items-center gap-3">
                            <motion.div
                              className={`p-2 rounded-lg ${alertLevel === 'critical' ? 'bg-red-500/20' :
                                alertLevel === 'warning' ? 'bg-orange-500/20' :
                                  'bg-green-500/20'
                                }`}
                              whileHover={{ scale: 1.1, rotate: 5 }}
                            >
                              <Smartphone className={`h-5 w-5 ${alertLevel === 'critical' ? 'text-red-400' :
                                alertLevel === 'warning' ? 'text-orange-400' :
                                  'text-green-400'
                                }`} />
                            </motion.div>
                            <div>
                              <p className="font-semibold text-white">{tech.technicianName}</p>
                              <p className="text-xs text-gray-400">{tech.city || 'غير محدد'}</p>
                            </div>
                          </div>
                          <div className="text-right">
                            <div className="flex items-center gap-2 mb-1">
                              <span className="text-2xl font-bold text-[#18B2B0]">{efficiency}%</span>
                            </div>
                            {alertLevel === 'critical' && (
                              <Badge className="bg-red-500/20 text-red-400 border-red-500/30 text-xs">
                                حرج
                              </Badge>
                            )}
                            {alertLevel === 'warning' && (
                              <Badge className="bg-orange-500/20 text-orange-400 border-orange-500/30 text-xs">
                                تحذير
                              </Badge>
                            )}
                          </div>
                        </div>
                        <div className="mt-3">
                          <div className="h-2 bg-gray-800 rounded-full overflow-hidden">
                            <motion.div
                              className={`h-full transition-all duration-500 ${alertLevel === 'critical' ? 'bg-gradient-to-r from-red-500 to-red-600' :
                                alertLevel === 'warning' ? 'bg-gradient-to-r from-orange-500 to-orange-600' :
                                  'bg-gradient-to-r from-[#18B2B0] to-[#0ea5a3]'
                                }`}
                              initial={{ width: 0 }}
                              animate={{ width: `${efficiency}%` }}
                              transition={{ duration: 1, delay: index * 0.1 }}
                            />
                          </div>
                        </div>
                      </motion.div>
                    );
                  })}

                  {/* Bar Chart for Technicians Efficiency */}
                  {techniciansData.technicians.length > 0 && (
                    <motion.div
                      initial={{ opacity: 0, y: 20 }}
                      animate={{ opacity: 1, y: 0 }}
                      transition={{ delay: 0.5 }}
                      className="bg-white/5 backdrop-blur-sm rounded-xl border border-white/10 p-4 mt-4"
                    >
                      <h3 className="text-sm font-semibold text-white mb-3">مخطط كفاءة الفنيين</h3>
                      <ChartContainer
                        config={{
                          efficiency: {
                            label: "الكفاءة",
                            color: "#18B2B0",
                          },
                        }}
                        className="h-[200px] w-full"
                      >
                        <BarChart
                          data={techniciansData.technicians.slice(0, 5).map(tech => ({
                            name: tech.technicianName.length > 8 ? tech.technicianName.substring(0, 8) + '...' : tech.technicianName,
                            efficiency: getTechnicianEfficiency(tech),
                            fullName: tech.technicianName
                          }))}
                          margin={{ top: 10, right: 10, left: 10, bottom: 10 }}
                        >
                          <XAxis
                            dataKey="name"
                            tick={{ fill: '#9ca3af', fontSize: 12 }}
                            axisLine={{ stroke: '#374151' }}
                          />
                          <YAxis
                            tick={{ fill: '#9ca3af', fontSize: 12 }}
                            axisLine={{ stroke: '#374151' }}
                            domain={[0, 100]}
                          />
                          <ChartTooltip
                            content={({ active, payload }) => {
                              if (active && payload && payload.length) {
                                return (
                                  <div className="bg-[#0f0f15] border border-[#18B2B0]/30 rounded-lg p-2 shadow-xl">
                                    <p className="text-white font-semibold">{payload[0].payload.fullName}</p>
                                    <p className="text-[#18B2B0]">{`الكفاءة: ${payload[0].value}%`}</p>
                                  </div>
                                );
                              }
                              return null;
                            }}
                          />
                          <Bar dataKey="efficiency" radius={[8, 8, 0, 0]}>
                            {techniciansData.technicians.slice(0, 5).map((tech, index) => {
                              const alertLevel = tech.alertLevel || 'good';
                              const color = alertLevel === 'critical' ? '#ef4444' :
                                alertLevel === 'warning' ? '#f59e0b' : '#18B2B0';
                              return <Cell key={`cell-${index}`} fill={color} />;
                            })}
                          </Bar>
                        </BarChart>
                      </ChartContainer>
                    </motion.div>
                  )}
                </>
              ) : (
                <div className="bg-white/5 backdrop-blur-sm rounded-xl border border-white/10 p-4">
                  <div className="flex items-center gap-3">
                    <div className="p-2 bg-green-500/20 rounded-lg">
                      <CheckCircle className="h-5 w-5 text-green-400" />
                    </div>
                    <div>
                      <p className="font-semibold text-white">{user?.fullName}</p>
                      <p className="text-xs text-gray-400">حسابك نشط</p>
                    </div>
                    <div className="mr-auto text-right">
                      <span className="text-2xl font-bold text-[#18B2B0]">95%</span>
                    </div>
                  </div>
                </div>
              )}
            </div>
          </motion.div>

          {/* Warehouses */}
          <motion.div
            initial={{ opacity: 0, y: 20 }}
            animate={{ opacity: 1, y: 0 }}
            transition={{ delay: 0.4 }}
            className="bg-gradient-to-br from-white/10 to-white/[0.03] backdrop-blur-xl rounded-3xl border border-[#18B2B0]/30 p-6 shadow-xl"
          >
            <div className="flex items-center gap-3 mb-6">
              <div className="p-2 bg-gradient-to-br from-[#18B2B0] to-[#0ea5a3] rounded-lg">
                <Warehouse className="h-5 w-5 text-white" />
              </div>
              <h2 className="text-xl font-bold text-white">مخزون المستودعات</h2>
            </div>

            <div className="space-y-4">
              {user?.role === 'admin' && warehousesData ? (
                <>
                  {warehousesData.slice(0, 2).map((warehouse, index) => {
                    const totalUnits = getWarehouseTotalUnits(warehouse);
                    const capacity = 200000;
                    const percentage = Math.min(100, Math.round((totalUnits / capacity) * 100));
                    const statusColor = percentage > 80 ? '#ef4444' : percentage > 50 ? '#f59e0b' : '#18B2B0';

                    return (
                      <motion.div
                        key={warehouse.id}
                        initial={{ opacity: 0, x: 20 }}
                        animate={{ opacity: 1, x: 0 }}
                        transition={{ delay: 0.5 + index * 0.1 }}
                        className="bg-white/5 backdrop-blur-sm rounded-xl border border-white/10 p-4 hover:border-[#18B2B0]/30 transition-all"
                      >
                        <div className="flex items-center justify-between mb-4">
                          <div>
                            <h3 className="font-bold text-white">{warehouse.name}</h3>
                            <p className="text-xs text-gray-400">قطاع {index === 0 ? 'أ' : 'ب'}</p>
                          </div>
                          <Badge
                            className={`${percentage > 80 ? 'bg-red-500/20 text-red-400 border-red-500/30' :
                                percentage > 50 ? 'bg-orange-500/20 text-orange-400 border-orange-500/30' :
                                  'bg-[#18B2B0]/20 text-[#18B2B0] border-[#18B2B0]/30'
                              }`}
                          >
                            {percentage}%
                          </Badge>
                        </div>

                        <div className="flex items-center gap-6">
                          <div className="flex-shrink-0">
                            <CircularProgress
                              percentage={percentage}
                              size={120}
                              strokeWidth={8}
                              label="السعة"
                              value={`${percentage}%`}
                              color={statusColor}
                            />
                          </div>

                          <div className="flex-1 space-y-2">
                            <div className="bg-white/5 rounded-lg p-2">
                              <p className="text-xs text-gray-400">إجمالي الوحدات</p>
                              <p className="text-lg font-bold text-white">{totalUnits.toLocaleString()}</p>
                            </div>
                            <div className="bg-white/5 rounded-lg p-2">
                              <p className="text-xs text-gray-400">آخر تحديث</p>
                              <p className="text-sm text-gray-300">منذ 24 ساعة</p>
                            </div>
                          </div>
                        </div>
                      </motion.div>
                    );
                  })}

                  {/* Bar Chart for Warehouses Capacity */}
                  {warehousesData.length > 0 && (
                    <motion.div
                      initial={{ opacity: 0, y: 20 }}
                      animate={{ opacity: 1, y: 0 }}
                      transition={{ delay: 0.7 }}
                      className="bg-white/5 backdrop-blur-sm rounded-xl border border-white/10 p-4 mt-4"
                    >
                      <h3 className="text-sm font-semibold text-white mb-3">مخطط سعة المستودعات</h3>
                      <ChartContainer
                        config={{
                          capacity: {
                            label: "السعة",
                            color: "#18B2B0",
                          },
                        }}
                        className="h-[200px] w-full"
                      >
                        <BarChart
                          data={warehousesData.map((warehouse, index) => {
                            const totalUnits = getWarehouseTotalUnits(warehouse);
                            const capacity = 200000;
                            const percentage = Math.min(100, Math.round((totalUnits / capacity) * 100));
                            return {
                              name: warehouse.name.length > 8 ? warehouse.name.substring(0, 8) + '...' : warehouse.name,
                              capacity: percentage,
                              fullName: warehouse.name,
                              totalUnits
                            };
                          })}
                          margin={{ top: 10, right: 10, left: 10, bottom: 10 }}
                        >
                          <XAxis
                            dataKey="name"
                            tick={{ fill: '#9ca3af', fontSize: 12 }}
                            axisLine={{ stroke: '#374151' }}
                          />
                          <YAxis
                            tick={{ fill: '#9ca3af', fontSize: 12 }}
                            axisLine={{ stroke: '#374151' }}
                            domain={[0, 100]}
                          />
                          <ChartTooltip
                            content={({ active, payload }) => {
                              if (active && payload && payload.length) {
                                return (
                                  <div className="bg-[#0f0f15] border border-[#18B2B0]/30 rounded-lg p-2 shadow-xl">
                                    <p className="text-white font-semibold">{payload[0].payload.fullName}</p>
                                    <p className="text-[#18B2B0]">{`السعة: ${payload[0].value}%`}</p>
                                    <p className="text-gray-400 text-xs">{`الوحدات: ${payload[0].payload.totalUnits.toLocaleString()}`}</p>
                                  </div>
                                );
                              }
                              return null;
                            }}
                          />
                          <Bar dataKey="capacity" radius={[8, 8, 0, 0]}>
                            {warehousesData.map((warehouse, index) => {
                              const totalUnits = getWarehouseTotalUnits(warehouse);
                              const capacity = 200000;
                              const percentage = Math.min(100, Math.round((totalUnits / capacity) * 100));
                              const color = percentage > 80 ? '#ef4444' : percentage > 50 ? '#f59e0b' : '#18B2B0';
                              return <Cell key={`cell-${index}`} fill={color} />;
                            })}
                          </Bar>
                        </BarChart>
                      </ChartContainer>
                    </motion.div>
                  )}
                </>
              ) : (
                <div className="bg-white/5 backdrop-blur-sm rounded-xl border border-white/10 p-6 text-center">
                  <Warehouse className="h-12 w-12 mx-auto mb-2 text-gray-500 opacity-50" />
                  <p className="text-gray-400">لا توجد مستودعات متاحة</p>
                </div>
              )}
            </div>
          </motion.div>
        </div>

        {/* Enhanced Critical Alerts */}
        {user?.role === 'admin' && techniciansData?.technicians?.some(t => t.alertLevel === 'critical') && (
          <motion.div
            initial={{ opacity: 0, scale: 0.95, y: 20 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            transition={{ delay: 0.6, type: "spring" }}
            className="mt-8 bg-gradient-to-r from-red-500/10 via-red-500/5 to-transparent border border-red-500/30 rounded-2xl p-6 backdrop-blur-xl shadow-2xl relative overflow-hidden"
          >
            {/* Animated background glow */}
            <motion.div
              className="absolute inset-0 bg-gradient-to-r from-red-500/20 to-transparent"
              animate={{
                opacity: [0.3, 0.6, 0.3],
              }}
              transition={{ duration: 2, repeat: Infinity }}
            />

            <div className="relative flex items-center gap-3 flex-wrap">
              <motion.div
                animate={{
                  scale: [1, 1.3, 1],
                  opacity: [1, 0.7, 1],
                  rotate: [0, 10, -10, 0]
                }}
                transition={{ duration: 2, repeat: Infinity }}
                className="p-3 bg-red-500/20 rounded-lg"
              >
                <AlertTriangle className="h-6 w-6 text-red-400" />
              </motion.div>
              <div className="flex-1">
                <motion.p
                  className="text-red-400 font-bold text-lg"
                  animate={{ opacity: [1, 0.8, 1] }}
                  transition={{ duration: 1.5, repeat: Infinity }}
                >
                  تنبيه حرج
                </motion.p>
                <p className="text-sm text-gray-400">عدة فنيين لديهم مستويات مخزون حرجة</p>
              </div>
              <Link href="/admin-inventory-overview">
                <motion.div
                  whileHover={{ scale: 1.05 }}
                  whileTap={{ scale: 0.95 }}
                >
                  <Button
                    className="bg-red-500 hover:bg-red-600 text-white shadow-lg shadow-red-500/30"
                  >
                    عرض التفاصيل
                    <ArrowRight className="h-4 w-4 mr-2" />
                  </Button>
                </motion.div>
              </Link>
            </div>
          </motion.div>
        )}

        {/* Low Stock Alerts */}
        {user?.role === 'admin' && dashboardStats && dashboardStats.lowStockItems > 0 && (
          <motion.div
            initial={{ opacity: 0, scale: 0.95, y: 20 }}
            animate={{ opacity: 1, scale: 1, y: 0 }}
            transition={{ delay: 0.7, type: "spring" }}
            className="mt-4 bg-gradient-to-r from-orange-500/10 via-orange-500/5 to-transparent border border-orange-500/30 rounded-2xl p-4 backdrop-blur-xl shadow-xl"
          >
            <div className="flex items-center gap-3">
              <motion.div
                animate={{
                  scale: [1, 1.2, 1],
                }}
                transition={{ duration: 2, repeat: Infinity }}
                className="p-2 bg-orange-500/20 rounded-lg"
              >
                <AlertTriangle className="h-5 w-5 text-orange-400" />
              </motion.div>
              <div className="flex-1">
                <p className="text-orange-400 font-semibold">مخزون منخفض</p>
                <p className="text-xs text-gray-400">{dashboardStats.lowStockItems} صنف يحتاج إلى إعادة تموين</p>
              </div>
            </div>
          </motion.div>
        )}
      </div>
    </div>
  );
}



