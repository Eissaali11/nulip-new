# 🔄 تحديث السيرفر - إصلاح خطأ تسجيل الدخول

## ✅ التغييرات المُطبقة:

1. **إضافة CORS middleware** - للسماح بطلبات API من Frontend
2. **تفعيل Trust Proxy** - للعمل خلف Nginx
3. **تحسين معالجة الأخطاء** - رسائل أوضح للمستخدم
4. **تحسين إعدادات Session** - دعم أفضل للـ HTTPS

---

## 🚀 خطوات التحديث على السيرفر:

### 1. الاتصال بالسيرفر:
```bash
ssh stoc@srv1233279.hostinger.com
cd ~/htdocs/stoc.fun
```

### 2. سحب التحديثات:
```bash
git pull origin main
```

### 3. تثبيت الحزم (إذا لزم الأمر):
```bash
npm ci
```

### 4. بناء التطبيق:
```bash
npm run build
```

### 5. إعادة تشغيل PM2:
```bash
pm2 restart nulip-inventory
```

### 6. التحقق من السجلات:
```bash
pm2 logs --lines 50
```

---

## 🔍 التحقق من الإصلاح:

1. افتح المتصفح: **https://stoc.fun**
2. جرب تسجيل الدخول
3. إذا استمرت المشكلة، تحقق من:
   - **PM2 logs**: `pm2 logs`
   - **Nginx logs**: `sudo tail -f /var/log/nginx/error.log`
   - **Environment variables**: تأكد من وجود `TRUST_PROXY=true` في `.env`

---

## 📝 ملاحظات:

- **CORS** الآن يدعم جميع الطلبات من نفس الـ origin
- **Trust Proxy** مفعل تلقائياً في production
- **Error messages** الآن بالعربية وأكثر وضوحاً

---

## 🆘 إذا استمرت المشكلة:

1. تحقق من أن **Nginx** يعمل:
   ```bash
   sudo systemctl status nginx
   ```

2. تحقق من أن **PM2** يعمل:
   ```bash
   pm2 status
   ```

3. تحقق من **Environment variables**:
   ```bash
   cat .env | grep TRUST_PROXY
   ```

4. تحقق من **Port 5000**:
   ```bash
   netstat -tulpn | grep 5000
   ```

---

**✅ بعد التحديث، يجب أن يعمل تسجيل الدخول بشكل صحيح!**
